---
# Not reloaded: ExecReload runs caddy reload, which posts to the admin
# API that admin off has closed. A restart drops in-flight video streams.
- name: Restart Caddy
  systemd:
    name: caddy
    state: restarted
    daemon_reload: true
