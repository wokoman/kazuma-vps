---
# Handlers for security_hardening role

# Reload, not restart: it runs sshd -t before signalling, and open sessions
# survive a config change that turns out to be wrong.
- name: reload ssh
  systemd:
    name: ssh
    state: reloaded

- name: restart fail2ban
  systemd:
    name: fail2ban
    state: restarted